    }

    private void navStyle(Button b,boolean active){ b.setBackground(bg(active?BLUE:PANEL_2,14,active?BLUE:BORDER,1)); b.setTextColor(active?Color.WHITE:MUTED); }
    private void showPanel(View target){
        homePanel.setVisibility(target==homePanel?View.VISIBLE:View.GONE); passengerPanel.setVisibility(target==passengerPanel?View.VISIBLE:View.GONE); settingsPanel.setVisibility(target==settingsPanel?View.VISIBLE:View.GONE); logsPanel.setVisibility(target==logsPanel?View.VISIBLE:View.GONE); browserPanel.setVisibility(target==browserPanel?View.VISIBLE:View.GONE);
        if(navHome!=null){ navStyle(navHome,target==homePanel); navStyle(navPassengers,target==passengerPanel); navStyle(navSettings,target==settingsPanel); navStyle(navLogs,target==logsPanel); }
    }

    private LinearLayout makeHomePanel(){
        ScrollView scroll=new ScrollView(this); scroll.setFillViewport(true); LinearLayout p=new LinearLayout(this); p.setOrientation(LinearLayout.VERTICAL); p.setPadding(dp(10),dp(8),dp(10),dp(16)); scroll.addView(p);
        LinearLayout route=section("مسیر سفر");
        LinearLayout rr=new LinearLayout(this); rr.setOrientation(LinearLayout.HORIZONTAL); origin=stationField("مبدا — تایپ یا انتخاب"); destination=stationField("مقصد — تایپ یا انتخاب"); Button swap=button("⇄",PANEL_2); rr.addView(origin,new LinearLayout.LayoutParams(0,dp(52),1)); gapH(rr,6); rr.addView(swap,new LinearLayout.LayoutParams(dp(54),dp(52))); gapH(rr,6); rr.addView(destination,new LinearLayout.LayoutParams(0,dp(52),1)); route.addView(rr); swap.setOnClickListener(v->{ String a=origin.getText().toString(),b=destination.getText().toString(); origin.setText(b); destination.setText(a); }); gap(route,8);
        LinearLayout dateRow=new LinearLayout(this); dateRow.setOrientation(LinearLayout.HORIZONTAL); Button cal=button("▣",PANEL_2); travelDate=field("تاریخ سفر"); travelDate.setFocusable(false); cal.setOnClickListener(v->showPersianDateDialog()); travelDate.setOnClickListener(v->showPersianDateDialog()); dateRow.addView(cal,new LinearLayout.LayoutParams(dp(56),dp(50))); gapH(dateRow,7); dateRow.addView(travelDate,new LinearLayout.LayoutParams(0,dp(50),1)); route.addView(dateRow); p.addView(route); gap(p,9);

        LinearLayout pass=section("تعداد مسافر"); pass.addView(makePassengerStepper("بزرگسال",true)); gap(pass,8); pass.addView(makePassengerStepper("کودک",false)); p.addView(pass); gap(p,9);

        LinearLayout account=section("حساب رجا"); LinearLayout ar=new LinearLayout(this); ar.setOrientation(LinearLayout.HORIZONTAL); phone=field("شماره موبایل رجا"); phone.setInputType(InputType.TYPE_CLASS_PHONE); password=field("رمز عبور"); password.setInputType(InputType.TYPE_CLASS_TEXT|InputType.TYPE_TEXT_VARIATION_PASSWORD); ar.addView(phone,new LinearLayout.LayoutParams(0,dp(50),1)); gapH(ar,7); ar.addView(password,new LinearLayout.LayoutParams(0,dp(50),1)); account.addView(ar); p.addView(account); gap(p,9);

        LinearLayout search=section("معیار جستجو"); trainNumber=field("شماره قطار"); trainNumber.setInputType(InputType.TYPE_CLASS_NUMBER); search.addView(trainNumber); priceMode=new CheckBox(this); priceMode.setText("جستجو با بازه قیمت"); priceMode.setTextColor(TEXT); search.addView(priceMode); LinearLayout prices=new LinearLayout(this); prices.setOrientation(LinearLayout.HORIZONTAL); minPrice=field("از قیمت"); minPrice.setInputType(InputType.TYPE_CLASS_NUMBER); maxPrice=field("تا قیمت"); maxPrice.setInputType(InputType.TYPE_CLASS_NUMBER); prices.addView(minPrice,new LinearLayout.LayoutParams(0,dp(50),1)); gapH(prices,7); prices.addView(maxPrice,new LinearLayout.LayoutParams(0,dp(50),1)); search.addView(prices); coupe=new CheckBox(this); coupe.setText("فقط کوپه دربست"); coupe.setTextColor(TEXT); alarm=new CheckBox(this); alarm.setText("آلارم پیدا شدن بلیت"); alarm.setTextColor(TEXT); alarm.setChecked(true); search.addView(coupe); search.addView(alarm); p.addView(search); gap(p,9);

        LinearLayout refreshBox=section("رفرش"); LinearLayout refRow=new LinearLayout(this); refRow.setOrientation(LinearLayout.HORIZONTAL); Button refIcon=button("↻",BLUE); refresh=field(""); refresh.setText("2"); refresh.setFocusable(false); refresh.setGravity(Gravity.CENTER); TextView sec=label("ثانیه"); sec.setGravity(Gravity.CENTER); View.OnClickListener open=v->showRefreshMenu(refIcon); refIcon.setOnClickListener(open); refresh.setOnClickListener(open); refRow.addView(refIcon,new LinearLayout.LayoutParams(dp(58),dp(50))); gapH(refRow,8); refRow.addView(refresh,new LinearLayout.LayoutParams(dp(72),dp(50))); refRow.addView(sec,new LinearLayout.LayoutParams(dp(70),dp(50))); refreshBox.addView(refRow); p.addView(refreshBox); gap(p,10);

        startBtn=button("⌕  جستجوی بلیت",BLUE); stopBtn=button("■ توقف",RED); stopBtn.setEnabled(false); p.addView(startBtn,new LinearLayout.LayoutParams(-1,dp(58))); gap(p,7); p.addView(stopBtn,new LinearLayout.LayoutParams(-1,dp(48))); status=label("● آماده"); status.setTextColor(GREEN); status.setGravity(Gravity.CENTER); p.addView(status,new LinearLayout.LayoutParams(-1,dp(40)));
        startBtn.setOnClickListener(v->startBot()); stopBtn.setOnClickListener(v->stopBot("توقف توسط کاربر")); priceMode.setOnCheckedChangeListener((b,c)->{ trainNumber.setEnabled(!c); minPrice.setEnabled(c); maxPrice.setEnabled(c); }); minPrice.setEnabled(false); maxPrice.setEnabled(false);
        LinearLayout c=new LinearLayout(this); c.setOrientation(LinearLayout.VERTICAL); c.addView(scroll,new LinearLayout.LayoutParams(-1,-1)); return c;
    }

    private LinearLayout makePassengerStepper(String text,boolean adult){
        LinearLayout row=new LinearLayout(this); row.setOrientation(LinearLayout.HORIZONTAL); row.setGravity(Gravity.CENTER_VERTICAL); row.setLayoutDirection(View.LAYOUT_DIRECTION_LTR); row.setBackground(bg(FIELD,14,BORDER,1)); row.setPadding(dp(10),dp(5),dp(10),dp(5)); TextView l=label(text); l.setGravity(Gravity.LEFT|Gravity.CENTER_VERTICAL); l.setTextSize(16); l.setTypeface(null,1); row.addView(l,new LinearLayout.LayoutParams(0,dp(48),1)); Button minus=button("−",PANEL_2); Button plus=button("+",BLUE); EditText val=field(""); val.setGravity(Gravity.CENTER); val.setText(adult?"1":"0"); val.setFocusable(false); if(adult)adults=val; else children=val; minus.setOnClickListener(v->{ int n=parseInt(val.getText().toString(),adult?1:0); val.setText(String.valueOf(Math.max(adult?1:0,n-1))); }); plus.setOnClickListener(v->{ int n=parseInt(val.getText().toString(),adult?1:0); val.setText(String.valueOf(Math.min(9,n+1))); }); row.addView(minus,new LinearLayout.LayoutParams(dp(50),dp(44))); gapH(row,5); row.addView(val,new LinearLayout.LayoutParams(dp(56),dp(44))); gapH(row,5); row.addView(plus,new LinearLayout.LayoutParams(dp(50),dp(44))); return row;
    }

    private void showRefreshMenu(View anchor){ PopupMenu m=new PopupMenu(this,anchor); for(int i=1;i<=10;i++)m.getMenu().add(String.valueOf(i)); m.setOnMenuItemClickListener(item->{ refresh.setText(item.getTitle().toString()); return true; }); m.show(); }

    private void showPersianDateDialog(){
        LinearLayout box=new LinearLayout(this); box.setOrientation(LinearLayout.HORIZONTAL); box.setPadding(dp(10),dp(8),dp(10),dp(4)); NumberPicker y=new NumberPicker(this),m=new NumberPicker(this),d=new NumberPicker(this); y.setMinValue(1404); y.setMaxValue(1412); m.setMinValue(1); m.setMaxValue(12); d.setMinValue(1); d.setMaxValue(31); try{ String[] a=travelDate.getText().toString().split("/"); if(a.length==3){ y.setValue(Integer.parseInt(a[0])); m.setValue(Integer.parseInt(a[1])); d.setValue(Integer.parseInt(a[2])); } else { y.setValue(1405); m.setValue(6); d.setValue(29); } }catch(Exception e){ y.setValue(1405); m.setValue(6); d.setValue(29); } box.addView(d,new LinearLayout.LayoutParams(0,-2,1)); box.addView(m,new LinearLayout.LayoutParams(0,-2,1)); box.addView(y,new LinearLayout.LayoutParams(0,-2,1)); new AlertDialog.Builder(this).setTitle("انتخاب تاریخ شمسی").setView(box).setNegativeButton("لغو",null).setPositiveButton("تأیید",(x,w)->travelDate.setText(String.format(Locale.US,"%04d/%02d/%02d",y.getValue(),m.getValue(),d.getValue()))).show();
    }

    private LinearLayout makePassengerPanel(){
        ScrollView scroll=new ScrollView(this); LinearLayout p=new LinearLayout(this); p.setOrientation(LinearLayout.VERTICAL); p.setPadding(dp(10),dp(8),dp(10),dp(18)); scroll.addView(p); LinearLayout form=section("مدیریت مسافران"); passengerName=field("نام"); passengerFamily=field("نام خانوادگی"); passengerNational=field("کد ملی"); passengerNational.setInputType(InputType.TYPE_CLASS_NUMBER); passengerBirth=field("تاریخ تولد 1370/01/01"); form.addView(passengerName); gap(form,6); form.addView(passengerFamily); gap(form,6); form.addView(passengerNational); gap(form,6); form.addView(passengerBirth); gap(form,8); Button add=button("+ افزودن مسافر",BLUE); form.addView(add); p.addView(form); gap(p,8); passengerList=section("مسافران ثبت‌شده"); p.addView(passengerList); passengerCountLabel=label("مسافر ذخیره‌شده: 0"); p.addView(passengerCountLabel); add.setOnClickListener(v->addPassenger()); LinearLayout c=new LinearLayout(this); c.setOrientation(LinearLayout.VERTICAL); c.addView(scroll,new LinearLayout.LayoutParams(-1,-1)); return c;
    }

    private LinearLayout makeSettingsPanel(){
        ScrollView scroll=new ScrollView(this); LinearLayout p=new LinearLayout(this); p.setOrientation(LinearLayout.VERTICAL); p.setPadding(dp(10),dp(8),dp(10),dp(18)); scroll.addView(p); LinearLayout box=section("تنظیمات حساب و سایت رجا"); TextView info=label("اطلاعات ورود در همین برنامه ذخیره می‌شود. برای خروج کامل، کوکی و نشست رجا پاک می‌شود."); info.setTextColor(MUTED); box.addView(info); gap(box,8); Button open=button("▣ نمایش سایت رجا",BLUE); Button logout=button("↪ خروج از حساب رجا",RED); box.addView(open,new LinearLayout.LayoutParams(-1,dp(50))); gap(box,7); box.addView(logout,new LinearLayout.LayoutParams(-1,dp(50))); p.addView(box); open.setOnClickListener(v->showPanel(browserPanel)); logout.setOnClickListener(v->logoutRaja()); LinearLayout c=new LinearLayout(this); c.setOrientation(LinearLayout.VERTICAL); c.addView(scroll,new LinearLayout.LayoutParams(-1,-1)); return c;
    }

    private LinearLayout makeLogsPanel(){
        LinearLayout p=new LinearLayout(this); p.setOrientation(LinearLayout.VERTICAL); p.setPadding(dp(10),dp(8),dp(10),dp(14)); LinearLayout top=new LinearLayout(this); top.setOrientation(LinearLayout.HORIZONTAL); Button copy=button("کپی لاگ",BLUE); Button clear=button("پاک کردن",PANEL_2); top.addView(clear,new LinearLayout.LayoutParams(0,dp(46),1)); gapH(top,6); top.addView(copy,new LinearLayout.LayoutParams(0,dp(46),1)); p.addView(top); gap(p,8); logView=new TextView(this); logView.setTextColor(TEXT); logView.setTextSize(12); logView.setGravity(Gravity.RIGHT); logView.setTextIsSelectable(true); logView.setBackground(bg(FIELD,14,BORDER,1)); logView.setPadding(dp(10),dp(10),dp(10),dp(10)); p.addView(logView,new LinearLayout.LayoutParams(-1,0,1)); copy.setOnClickListener(v->{ ClipboardManager cm=(ClipboardManager)getSystemService(Context.CLIPBOARD_SERVICE); if(cm!=null){ cm.setPrimaryClip(ClipData.newPlainText("MASOUD Raja Logs",logView.getText())); toast("لاگ کپی شد"); }}); clear.setOnClickListener(v->logView.setText("")); return p;
    }

    private LinearLayout makeBrowserPanel(){ LinearLayout p=new LinearLayout(this); p.setOrientation(LinearLayout.VERTICAL); p.setPadding(dp(5),dp(4),dp(5),dp(5)); TextView h=label("سایت رجا — هنگام پیدا شدن بلیت خودکار باز می‌شود"); h.setGravity(Gravity.CENTER); h.setTextColor(BLUE); p.addView(h,new LinearLayout.LayoutParams(-1,dp(36))); webView=new WebView(this); webView.setBackgroundColor(Color.WHITE); p.addView(webView,new LinearLayout.LayoutParams(-1,0,1)); return p; }

    private void setupWebView(){
