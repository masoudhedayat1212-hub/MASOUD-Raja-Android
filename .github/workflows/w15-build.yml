name: W15 Build

permissions:
  contents: write

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/w15-build.yml'
      - 'w15/MainActivity.java'
      - 'w2/header_parts_v2/**'
      - 'w2/icon_w7_parts/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Prepare W15 from W4 verified reference
        shell: bash
        run: |
          set -euo pipefail
          rm -rf buildsrc output
          unzip -q MASOUD_Android_v1.zip -d buildsrc
          APP=buildsrc/MASOUD_Android
          MAIN="$APP/app/src/main/java/com/masoud/raja/MainActivity.java"
          cp w15/MainActivity.java "$MAIN"

          sed -i 's/import android.app.Ringtone;/import android.media.Ringtone;/' "$MAIN"
          sed -i 's/import android.app.RingtoneManager;/import android.media.RingtoneManager;/' "$MAIN"

          mkdir -p "$APP/app/src/main/res/drawable-nodpi"
          python3 - <<'PY'
          import base64
          from pathlib import Path
          source = ''.join(p.read_text(encoding='utf-8') for p in sorted(Path('w2/header_parts_v2').glob('part_*.b64')))
          clean = ''.join(ch for ch in source if ch.isalnum() or ch in '+/=')
          clean += '=' * (-len(clean) % 4)
          image = base64.b64decode(clean, validate=False)
          if image[:3] != bytes((255, 216, 255)):
              raise SystemExit('Invalid JPEG header')
          Path('buildsrc/MASOUD_Android/app/src/main/res/drawable-nodpi/w2_header_reference.jpg').write_bytes(image)
          PY
          test -s "$APP/app/src/main/res/drawable-nodpi/w2_header_reference.jpg"
          file "$APP/app/src/main/res/drawable-nodpi/w2_header_reference.jpg"
          python3 - <<'PY'
          import base64
          from pathlib import Path
          source = ''.join(p.read_text(encoding='utf-8') for p in sorted(Path('w2/icon_w7_parts').glob('part_*.b64')))
          clean = ''.join(ch for ch in source if ch.isalnum() or ch in '+/=')
          clean += '=' * (-len(clean) % 4)
          image = base64.b64decode(clean, validate=False)
          if image[:3] != bytes((255, 216, 255)):
              raise SystemExit('Invalid W15 icon JPEG')
          Path('buildsrc/MASOUD_Android/app/src/main/res/drawable-nodpi/w12_train_icon_source.jpg').write_bytes(image)
          PY
          test -s "$APP/app/src/main/res/drawable-nodpi/w12_train_icon_source.jpg"
          file "$APP/app/src/main/res/drawable-nodpi/w12_train_icon_source.jpg"
          python3 -m pip install --quiet pillow
          python3 - <<'PY'
          from PIL import Image, ImageOps
          from pathlib import Path
          src=Image.open('buildsrc/MASOUD_Android/app/src/main/res/drawable-nodpi/w12_train_icon_source.jpg').convert('RGB')
          icon=ImageOps.fit(src,(512,512),method=Image.Resampling.LANCZOS,centering=(0.43,0.5))
          icon.save('buildsrc/MASOUD_Android/app/src/main/res/drawable-nodpi/w12_train_icon.png',optimize=True)
          PY
          test -s "$APP/app/src/main/res/drawable-nodpi/w12_train_icon.png"
          python3 - <<'PY'
          import re
          from pathlib import Path
          p=Path('buildsrc/MASOUD_Android/app/src/main/AndroidManifest.xml')
          t=p.read_text(encoding='utf-8')
          t=re.sub(r'\s+android:icon="[^"]*"','',t)
          t=re.sub(r'\s+android:roundIcon="[^"]*"','',t)
          t=t.replace('<application','<application android:icon="@drawable/w12_train_icon" android:roundIcon="@drawable/w12_train_icon"',1)
          p.write_text(t,encoding='utf-8')
          PY
          grep -q 'android:icon="@drawable/w12_train_icon"' "$APP/app/src/main/AndroidManifest.xml"
          grep -q 'android:roundIcon="@drawable/w12_train_icon"' "$APP/app/src/main/AndroidManifest.xml"

          python3 - <<'PY'
          from pathlib import Path
          p=Path('buildsrc/MASOUD_Android/app/build.gradle')
          t=p.read_text(encoding='utf-8')
          t=t.replace("applicationId 'com.masoud.raja'","applicationId 'com.masoud.raja.w15'")
          t=t.replace('applicationId "com.masoud.raja"','applicationId "com.masoud.raja.w15"')
          t=t.replace('versionCode 1','versionCode 45')
          t=t.replace("versionName '1.0.0'","versionName 'W15'")
          t=t.replace('versionName "1.0.0"','versionName "W15"')
          p.write_text(t,encoding='utf-8')
          PY

      - name: Verify approved changes only
        shell: bash
        run: |
          set -euo pipefail
          grep -q 'R.drawable.w2_header_reference' w15/MainActivity.java
          ! grep -q 'MASOUD Raja Bot' w15/MainActivity.java
          ! grep -q 'tabPassengers' w15/MainActivity.java
          grep -q 'showPassengerDialog()' w15/MainActivity.java
          grep -q 'version.setText("W15")' w15/MainActivity.java
          grep -q 'injectSearchFormV11()' w15/MainActivity.java
          grep -q 'منتظر تکمیل ورود' w15/MainActivity.java
          grep -q 'logScroll.fullScroll' w15/MainActivity.java
          ! grep -q 'نوزاد' w15/MainActivity.java
          grep -q 'jalaliToGregorian' w15/MainActivity.java
          grep -q 'DAY_OF_WEEK)%7' w15/MainActivity.java
          grep -q "تعداد بزرگسال و کودک در رجا تنظیم شد" w15/MainActivity.java
          grep -q 'enablePriceFormatting' w15/MainActivity.java
          grep -q 'loginMissing' w15/MainActivity.java
          grep -q 'MutationObserver' w15/MainActivity.java
          grep -q 'پاک‌کردن لاگ' w15/MainActivity.java
          grep -q 'refreshSlider' w15/MainActivity.java
          grep -q 'selectionError' w15/MainActivity.java
          grep -q 'setDropDownBackgroundDrawable' w15/MainActivity.java
          grep -q 'new WebView(this)' w15/MainActivity.java
          grep -q 'injectLogin()' w15/MainActivity.java
          grep -q 'injectSearchForm()' w15/MainActivity.java
          grep -q 'inspectResultsAndReserve()' w15/MainActivity.java

      - name: Build APK
        shell: bash
        run: |
          set -euo pipefail
          cd buildsrc/MASOUD_Android
          gradle assembleDebug --stacktrace

      - name: Verify APK
        shell: bash
        run: |
          set -euo pipefail
          APK=$(find buildsrc/MASOUD_Android/app/build/outputs/apk -type f -name '*.apk' | head -1)
          test -s "$APK"
          unzip -t "$APK" >/dev/null
          mkdir -p "$GITHUB_WORKSPACE/output"
          cp "$APK" "$GITHUB_WORKSPACE/output/w15.apk"

      - name: Save W15 APK in repository
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p releases
          cp output/w15.apk releases/MASOUD_Raja_W15.apk
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -f releases/MASOUD_Raja_W15.apk
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "Save installable W15 APK"
          git push

      - uses: actions/upload-artifact@v4
        with:
          name: w15
          path: output/w15.apk
          if-no-files-found: error
